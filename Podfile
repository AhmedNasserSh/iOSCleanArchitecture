# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'iOS Clean Architecture' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for iOS Clean Architecture
  pod 'HydraAsync'
  pod 'SwiftyJSON', '~> 4.0'
  pod 'ObjectMapper'
  pod 'SDWebImage'
  pod 'KVNProgress'
  pod 'Alamofire'
 target 'iOS Clean ArchitectureTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
